# Nixpacks configuration for Railway Backend Only
# Ensures Railway uses Node 20 and stays in backend directory

[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm install --production']

[start]
cmd = 'node src/index.js'
